services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
    volumes:
      - ./frontend:/app

  backend:
    build: ./backend
    ports:
      - "4000:4000"
